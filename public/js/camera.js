// Generated by CoffeeScript 1.3.3
(function() {
  $(function() {
    var e, t, n, r, i, s, o, u, a, f, l, c, h, p, d, v, m, g, y;
    t = $('input[type="file"]');
    p = $(".shutter");
    n = $("canvas");
    f = $("#photo-image");
    h = $(".share");
    u = $(".locate");
    r = $("textarea");
    i = $(".caption");
    o = $(".counter");
    e = $(".prompt");
    m = $(".spinnerContainer");
    g = 0;
    y = void 0;
    a = void 0;
    v = {
      lines: 12,
      length: 11,
      width: 4,
      radius: 12,
      color: "#FFF",
      hwaccel: !0
    };
    d = new Spinner(v);
    "standalone" in window.navigator && !window.navigator.standalone && e.show();
    p.on("touchend", function() {
      t.trigger("click");
      $.get("/twitter_config", function(e) {
        var t;
        g = 140 - parseInt($.parseJSON(e).characters_reserved_per_media, 10);
        0 < g && g < 140 || (g = 119);
        r.attr("maxLength", g);
        t = r.val() === "" ? r.attr("placeholder") : r.val();
        return o.text(t.length + "/" + g);
      });
      return _gaq.push([ "_trackEvent", "Photos", "Camera" ]);
    });
    r.on("keyup", function() {
      var e;
      e = r.val() === "" ? r.attr("placeholder") : r.val();
      return o.text(e.length + "/" + g);
    });
    t.on("change", function() {
      var t, r = this;
      e.hide();
      p.prop("disabled", !0);
      d.spin(m[0]);
      window.orientation === 0 ? t = window.innerHeight : t = window.innerWidth - p.height() / 4;
      return p.animate({
        marginTop: t
      }, "swing", function() {
        var e;
        n.show();
        a = r.files[0];
        y = a.type;
        e = new FileReader;
        e.onload = s;
        return e.readAsBinaryString(a);
      });
    });
    n.on("rendered", function() {
      return Caman("#photo-canvas", function() {
        return this.exposure(10).saturation(10).colorize(255, 200, 0, 10).vignette("40%", 20).render(function() {
          console.log("finished");
          d.stop();
          p.prop("disabled", !1);
          h.prop("disabled", !1);
          i.show();
          return f.prop("src", n[0].toDataURL()).show(function() {
            return n.hide();
          });
        });
      });
    });
    s = function() {
      var e, t;
      e = EXIF.readFromBinaryFile(new BinaryFile(this.result));
      t = new MegaPixImage(a);
      return t.render(n[0], {
        maxWidth: 1e3,
        maxHeight: 1e3,
        orientation: e.Orientation
      });
    };
    h.on("click", function() {
      var e = this;
      console.log("tweeting photo");
      n.is(":visible") && n.animate({
        marginTop: "-2000px"
      }, "fast", function() {
        f.hide();
        i.hide();
        d.spin(m[0]);
        p.fadeOut("fast");
        l();
        return h.prop("disabled", !0);
      });
      return _gaq.push([ "_trackEvent", "Photos", "Tweet" ]);
    });
    l = function() {
      var e, t, i, s;
      console.log("getting location");
      t = null;
      i = null;
      navigator.geolocation.getCurrentPosition(function(e) {
        t = e.coords.latitude;
        return i = e.coords.longitude;
      });
      (function(e) {
        switch (e.code) {
         case e.TIMEOUT:
          return console.log("Geolocation error: Timeout");
         case e.POSITION_UNAVAILABLE:
          return console.log("Geolocation error: Position unavailable");
         case e.PERMISSION_DENIED:
          return console.log("Geolocation error: Permission denied");
         case e.UNKNOWN_ERROR:
          return console.log("Geolocation error: Unknown error");
        }
      });
      e = n[0].toDataURL();
      console.log("posting status");
      s = r.val() === "" ? r.attr("placeholder") : r.val();
      return $.post("/tweet", {
        status: s,
        photo: e,
        lat: t,
        "long": i
      }, function() {
        d.stop();
        m.html('<i class="icon-ok-sign" style="font-size: 300%; color: white;"></i>');
        return window.setTimeout(function() {
          m.html("");
          return c();
        }, 1e3);
      });
    };
    return c = function() {
      p.fadeIn("fast");
      p.animate({
        marginTop: "50%"
      });
      n.css("margin-top", "0px");
      i.hide();
      r.val("");
      return t.val("");
    };
  });
}).call(this);